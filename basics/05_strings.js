const name = "Omprakash"
const repoCount = 200

// backtick string prinint technique used now days :
console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);
const gameName = new String("Omprakash") // string is not called array here , it is key value pair (dictionary concpet)
// we can access all methods of string using dot (.) notation

console.log(gameName[0])
console.log(gameName.__proto__);
console.log(gameName.length);
console.log(gameName.toUpperCase());
console.log(gameName.charAt(4));

// Notes: Its a good practice to write and check output of all the methods associated with the String object.



